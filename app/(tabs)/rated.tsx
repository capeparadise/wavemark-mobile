import React from 'react';
import { Text, View } from 'react-native';

// Archived: rated moved to app/_archive/rated.tsx
export default function ArchivedRated() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#666' }}>Archived — see /app/_archive/rated.tsx</Text>
    </View>
  );
}
