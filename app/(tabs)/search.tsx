import React from 'react';
import { Text, View } from 'react-native';

// Archived: search moved to app/_archive/search.tsx
export default function ArchivedSearch() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#666' }}>Archived — see /app/_archive/search.tsx</Text>
    </View>
  );
}
