import React from 'react';
import { Text, View } from 'react-native';

// Archived: add-release moved to app/_archive/add-release.tsx
export default function ArchivedAddRelease() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#666' }}>Archived — see /app/_archive/add-release.tsx</Text>
    </View>
  );
}
