import React from 'react';
import { Text, View } from 'react-native';

// Archived: upcoming moved to app/_archive/upcoming.tsx
export default function ArchivedUpcoming() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#666' }}>Archived — see /app/_archive/upcoming.tsx</Text>
    </View>
  );
}
