# Brand assets

Place the RPPL logo files here:

- `rppl-logo.png`: default/dark logo for light app themes.
- `rppl-logo-light.png`: light/white logo for dark app backgrounds.

Both files should use transparent backgrounds. Keep the same filenames so `components/BrandLogo.tsx` can bundle them with static `require(...)`.
